// import { Bar } from "react-chartjs-2";
// import { Chart as ChartJS } from "chart.js/auto";

// type BarChartProps = {
//   chartData: any;
//   chartOptions: any;
//   canvasId: string;
// };

// export function BarChart({ chartData, chartOptions,canvasId }: BarChartProps) {
//   return <Bar data={chartData} options={chartOptions} id={canvasId}/>;
// }
